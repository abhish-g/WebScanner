# CPU-only PyTorch. Without this line pip pulls the CUDA build,
# which is ~2.5GB and will blow up small free-tier containers.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Web ---
Flask==3.1.3
gunicorn==23.0.0
requests==2.32.3
python-dotenv==1.0.1

# --- ML / RAG ---
numpy==2.5.2
scikit-learn==1.7.2
joblib==1.5.3
faiss-cpu==1.15.0
torch==2.13.0+cpu
sentence-transformers==6.0.0

# Versions match the local environment where this was tested.
# scikit-learn in particular MUST match the version that produced
# model.pkl — unpickling across major versions warns or fails.
# Regenerate with `pip freeze` after any upgrade.